import { ScrollOpacityDirective } from './scroll-opacity.directive';

describe('ScrollOpacityDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollOpacityDirective();
    expect(directive).toBeTruthy();
  });
});
